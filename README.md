# Модуль "Учет финансов" — Полный отчет

> 🎯 Краткое описание  
> Модуль для хранения и аналитики личных/корпоративных финансов: счета, категории, операции, импорт/экспорт и аналитика. Построен с применением принципов SOLID и GRASP, использует шаблоны проектирования (GoF) и DI-контейнер.

---

## Содержание
- [Идея решения](#идея-решения)
- [Реализованный функционал](#реализованный-функционал)
- [Доменная модель](#доменная-модель)
- [Валидация](#валидация)
- [Аналитика](#аналитика)
- [Импорт / Экспорт](#импорт--экспорт)
- [Управление данными](#управление-данными)
- [Статистика производительности](#статистика-производительности)
- [Принципы проектирования (SOLID / GRASP)](#принципы-проектирования-solid--grasp)
- [Паттерны (GoF) и модули](#паттерны-gof-и-модули)
- [Запуск](#запуск)

---

## Идея решения
Реализовать модуль учета финансов с чистой доменной моделью, расширяемой аналитикой и гибкими модулями импорта/экспорта. Сильный акцент на читаемость, тестируемость и возможность расширения без изменения существующего кода.

---

## Реализованный функционал

### Основные требования (полностью реализованы)
- ✅ CRUD для счётов (BankAccount), категорий (Category) и операций (Operation)
- ✅ Полные классы доменной модели: `BankAccount`, `Category`, `Operation` — соответствуют спецификациям
- ✅ Валидация данных: запрет отрицательных сумм, пустых названий и др.

### Опциональная функциональность (реализована полностью)
- ✅ Аналитика:
  - Подсчет разницы доходов и расходов за период
  - Группировка по категориям
  - Общая статистика операций
- ✅ Импорт / Экспорт данных:
  - Экспорт: JSON, CSV, YAML
  - Импорт: JSON, CSV, YAML
- ✅ Управление данными:
  - Ручной пересчет баланса счетов
  - Автоматический пересчет при создании/удалении/изменении операций
- ✅ Статистика:
  - Измерение времени выполнения пользовательских сценариев (TimedCommandDecorator и т.п.)

---

## Доменная модель
- **BankAccount** — баланс, валюта, идентификатор, метаданные.
- **Category** — тип (доход/расход), родительские категории, метаданные.
- **Operation** — сумма, тип (income/expense/transfer), дата, связанный счёт, категория, описание.

Фабрика доменных объектов: `DomainFactory` — централизованное создание с валидацией.

---

## Валидация
- Запрет пустых названий (счетов / категорий / операций).
- Запрет отрицательных сумм (по умолчанию); переносы между счетами обрабатываются отдельно.
- Валидация дат и ссылок на существующие сущности.

---

## Аналитика
- `AnalyticsFacade` агрегирует стратегии аналитики:
  - `PeriodBalanceStrategy` — баланс за период
  - `CategoryAnalysisStrategy` — группировка и распределение по категориям
- Поддержка добавления новых стратегий через интерфейс `IAnalyticsStrategy`.

---

## Импорт / Экспорт
- **Экспорт:** реализованы посетители экспорта `IExportVisitor`:
  - `JSONExportVisitor`, `CSVExportVisitor`, `YAMLExportVisitor`
- **Импорт:** шаблонный метод `DataImporter` и конкретные импортеры:
  - `JSONDataImporter`, `CSVDataImporter`, `YAMLDataImporter`

---

## Управление данными
- Репозитории через интерфейсы (`IRepository`, `IBankAccountRepository`, `ICategoryRepository`, `IOperationRepository`).
- Proxy-репозиторий (`BankAccountRepositoryProxy`) — кэширование и контроль доступа.
- Возможность ручного и автоматического пересчета баланса.
- Событийная модель оповещений при изменении операций.

---

## Статистика производительности
- Декоратор `TimedCommandDecorator` измеряет время выполнения команд (паттерн Command + Decorator).
- Измерение и логирование в ключевых пользовательских сценариях.

---

## Принципы проектирования (SOLID / GRASP)

### SOLID
1. **SRP** — фасады и фабрики имеют одну ответственность (`BankAccountFacade`, `AnalyticsFacade`, `DomainFactory`).
2. **OCP** — стратегии аналитики, посетители экспорта и импортеры расширяемы без изменения существующего кода.
3. **LSP** — стратегии и посетители взаимозаменяемы.
4. **ISP** — разделение интерфейсов (`ICommand`, `IRepository`, `IAnalyticsStrategy`).
5. **DIP** — фасады и сервисы зависят от абстракций; DI-контейнер управляет зависимостями.

### GRASP
- **High Cohesion** — фасады и модули сфокусированы по зонам ответственности.
- **Low Coupling** — взаимодействие через интерфейсы, DI и события.
- Использованы паттерны для снижения связанности и повышения модульности.

---

## Паттерны (GoF) и соответствующие модули
1. **Фасад (Facade)**  
   `BankAccountFacade`, `CategoryFacade`, `OperationFacade`, `AnalyticsFacade`  
   Модули: `patterns/facade.py`, `services/analytics.py`
2. **Команда (Command)**  
   `ICommand`, `CreateAccountCommand`, `CreateOperationCommand`, `RecalculateBalanceCommand`  
   Модуль: `patterns/command.py`
3. **Декоратор (Decorator)**  
   `TimedCommandDecorator` — измерение времени исполнения команд  
   Модуль: `patterns/decorator.py`
4. **Шаблонный метод (Template Method)**  
   `DataImporter` и конкретные импортеры (`JSONDataImporter`, `CSVDataImporter`, `YAMLDataImporter`)  
   Модуль: `patterns/template_method.py`
5. **Посетитель (Visitor)**  
   `IExportVisitor`, `JSONExportVisitor`, `CSVExportVisitor`, `YAMLExportVisitor`  
   Модуль: `patterns/visitor.py`
6. **Фабрика (Factory)**  
   `DomainFactory` — централизованное создание объектов и валидация  
   Модуль: `patterns/factory.py`
7. **Прокси (Proxy)**  
   `BankAccountRepositoryProxy` — кэширование и контроль доступа  
   Модуль: `repositories/proxies.py`
8. **Repository**  
   `IRepository`, `BankAccountRepository`, `CategoryRepository`, `OperationRepository`  
   Модуль: `repositories/`
9. **Стратегия (Strategy)**  
   `IAnalyticsStrategy`, `PeriodBalanceStrategy`, `CategoryAnalysisStrategy`  
   Модуль: `patterns/strategy.py`
10. **DI-контейнер (Dependency Injection Container)**  
    `DIContainer` — конфигурация и создание зависимостей  
    Модуль: `patterns/di_container.py`

---

## Запуск

1. Установите Python 3.7+  
2. Установите зависимости:
pip install pyyaml
